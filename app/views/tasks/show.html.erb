<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<% if @task.completed %>
    <%= "☑️ This task is completed." %>
  <% else %>
    <%= "▢ This task is not completed yet." %>
<% end %>
<br>
<%= link_to 'Back to task list', index_path %>
